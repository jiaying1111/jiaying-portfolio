/** Homepage hero: one slide is shown for five seconds before advancing. */
export const HERO_AUTOPLAY_MS = 5000;

/** Homepage hero crossfade between slide images. */
export const HERO_CROSSFADE_MS = 600;

/** Homepage Experience card regular -> hover crossfade. */
export const CARD_CROSSFADE_MS = 300;

/** Experience / Artwork listing hover loop: frame-01 -> 02 -> 03 -> 01. */
export const HOVER_LOOP_INTERVAL_MS = 900;

/** Hover-loop frame crossfade. */
export const HOVER_LOOP_CROSSFADE_MS = 300;

/** Homepage Artwork rail: duration of one full eight-tile cycle. */
export const ARTWORK_RAIL_DURATION_MS = 45000;
