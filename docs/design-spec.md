# Design Specification Router

Status: **CURRENT**

The former partial extraction notes contained obsolete TBD items, a red
metadata color, and outdated typography assumptions. They are superseded.

Use sources in this order:

1. `docs/figma-reference/export-2026-08-26/` — current visual truth.
2. `docs/design-system.md` — typography, colors, spacing, geometry.
3. `docs/content.md` — visible wording and metadata.
4. `docs/media-library.md` — image paths and interaction sequences.
5. `docs/qa-report-2026-08-26.md` — defects only; current content wins if copy
   differs.

Do not use older reference PNGs directly under `docs/figma-reference/`. Do not
use `#FF2020` for metadata. Do not retain TBD or `This project etc...` copy.

